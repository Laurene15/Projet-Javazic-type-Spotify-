//
// Source code recreated from a .class file by IntelliJ IDEA
// (powered by Fernflower decompiler)
//

package model;

import java.io.File;
import java.io.FileInputStream;
import java.io.FileOutputStream;
import java.io.IOException;
import java.io.ObjectInputStream;
import java.io.ObjectOutputStream;

public class PersistanceManager {
    private static final String FICHIER_CATALOGUE = "catalogue.ser";
    private static final String FICHIER_UTILISATEURS = "utilisateurs.ser";

    private PersistanceManager() {
    }

    public static void sauvegarderCatalogue(Catalogue catalogue) {
        try (ObjectOutputStream oos = new ObjectOutputStream(new FileOutputStream("catalogue.ser"))) {
            oos.writeObject(catalogue);
        } catch (IOException e) {
            System.err.println("Erreur sauvegarde catalogue : " + e.getMessage());
        }

    }

    public static Catalogue chargerCatalogue() {
        File fichier = new File("catalogue.ser");
        if (!fichier.exists()) {
            return new Catalogue();
        } else {
            try (ObjectInputStream ois = new ObjectInputStream(new FileInputStream(fichier))) {
                return (Catalogue)ois.readObject();
            } catch (ClassNotFoundException | IOException e) {
                System.err.println("Erreur chargement catalogue : " + ((Exception)e).getMessage());
                return new Catalogue();
            }
        }
    }

    public static void sauvegarderUtilisateurs(GestionnaireUtilisateurs gestionnaire) {
        try (ObjectOutputStream oos = new ObjectOutputStream(new FileOutputStream("utilisateurs.ser"))) {
            oos.writeObject(gestionnaire);
        } catch (IOException e) {
            System.err.println("Erreur sauvegarde utilisateurs : " + e.getMessage());
        }

    }

    public static GestionnaireUtilisateurs chargerUtilisateurs() {
        File fichier = new File("utilisateurs.ser");
        if (!fichier.exists()) {
            return new GestionnaireUtilisateurs();
        } else {
            try (ObjectInputStream ois = new ObjectInputStream(new FileInputStream(fichier))) {
                return (GestionnaireUtilisateurs)ois.readObject();
            } catch (ClassNotFoundException | IOException e) {
                System.err.println("Erreur chargement utilisateurs : " + ((Exception)e).getMessage());
                return new GestionnaireUtilisateurs();
            }
        }
    }
}
